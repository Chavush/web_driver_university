{
  "name": "web_driver_university",
  "version": "1.0.0",
  "description": "Web driver university",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open:headed:chrome": "cypress open -b chrome --headed",
    "cy:run:headed:chrome": "cypress run -b chrome --headed"

  },
  "author": "Sergey Chavushyan",
  "license": "ISC",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^15.0.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^12.3.0",
    "typescript": "^4.9.4"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": "cypress/support/step_definitions/**/*.{js,ts}"
  }
}
